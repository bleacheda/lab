apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: deploy-httpd
  name: deploy-httpd
spec:
  replicas: 10
  selector:
    matchLabels:
      app: deploy-httpd
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: deploy-httpd
    spec:
      containers:
      - image: httpd:alpine3.19
        name: httpd
        resources: {}
status: {}
